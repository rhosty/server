{
  "version": 2,
  "builds": [
    {
      "src": "todo/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "todo/dist/index.html" }
  ]
}